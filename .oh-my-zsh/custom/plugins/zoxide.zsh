# =============================================================================
# zoxide plugin
# Smart directory jumping with z
# =============================================================================

# Initialize zoxide (adds 'z' and 'zi' commands)
eval "$(zoxide init zsh)"
